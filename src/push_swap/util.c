#include "push_swap.h"

static int	highest(t_list **stack)
{
	t_list	*temp_stack;
	int	temp;
}

void	rotate_max_on_top(t_list **stack)
{
	t_list	*temp_stack;
	int		target;

	target = highest(stack);
	while (temp_stack->content != target)

}
